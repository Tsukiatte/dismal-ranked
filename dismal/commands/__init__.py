"""Command implementations.

Each module holds the logic for a related group of slash commands. `main.py`
registers the commands and forwards to these functions, so the registration
boilerplate stays separate from what the commands actually do.
"""
